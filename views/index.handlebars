<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <h1>{{title}}</h1>
    <p>A simple tool for browsing Unicode characters, inspired by <a href="http://www.alanwood.net/">Alan Wood</a>.</p>

    <div id="controls">
      <label for="block">Block:</label>
      <select id="block">
        {{#each blocks}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
      </select>

      <br>

      <label for="search">Search</label>
      <input id="search" type="search" placeholder="enter a character name">
      <input id="block_only" type="checkbox">
      <label for="block_only">in this block only</label>


    </div>

    <div id="charlist" class="loading"></div>

    <script src="/js/lib/handlebars.runtime-v1.3.0.js"></script>
    <script src="/js/templates.js"></script>
    <script src="/js/main.js"></script>
  </body>
</html>
